<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/item_filter_option.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="52dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:clickable="true"
    android:focusable="true">

    <!-- Custom Checkbox -->
    <FrameLayout
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_marginEnd="14dp">

        <!-- Checkbox background (unchecked / checked controlled in adapter) -->
        <View
            android:id="@+id/view_checkbox"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/checkbox_unchecked_bg" />

        <!-- Check icon — visible only when selected -->
        <ImageView
            android:id="@+id/iv_check_icon"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_check_white"
            android:visibility="gone" />
    </FrameLayout>

    <!-- Name -->
    <TextView
        android:id="@+id/tv_option_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="#1A1A1A"
        android:textSize="14sp"
        android:maxLines="1"
        android:ellipsize="end" />

    <!-- Count -->
    <TextView
        android:id="@+id/tv_option_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#999999"
        android:textSize="12sp"
        android:layout_marginStart="8dp"
        android:visibility="gone" />

</LinearLayout>